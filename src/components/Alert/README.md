# Alert

`Alert` communicates important feedback without interrupting the current task.

```tsx
<Alert status="success" title="Saved">Your changes are live.</Alert>
```

Statuses: `info`, `success`, `warning`, and `error`.

