# Spinner

`Spinner` communicates an in-progress operation with an accessible status
label. It respects reduced-motion preferences.

```tsx
<Spinner label="Loading projects" />
```

Sizes: `sm`, `md`, and `lg`.

