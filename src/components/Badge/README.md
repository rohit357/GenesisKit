# Badge

`Badge` is a compact status label for categories, counts, and state.

```tsx
<Badge variant="success">Published</Badge>
```

Variants: `neutral`, `info`, `success`, `warning`, and `danger`.

