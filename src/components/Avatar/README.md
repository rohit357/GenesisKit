# Avatar

`Avatar` displays a profile image and falls back to initials when no image is
supplied or the image cannot load.

```tsx
<Avatar name="Ada Lovelace" src="/ada.jpg" />
```

Sizes: `sm`, `md`, and `lg`. Shapes: `circle` and `square`.

