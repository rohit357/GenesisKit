# Switch

`Switch` is a binary on/off control built on a native checkbox with switch
semantics.

```tsx
<Switch label="Email notifications" checked={enabled} onChange={onChange} />
```

Sizes: `sm` and `md`.

